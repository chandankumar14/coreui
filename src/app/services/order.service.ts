import { Injectable } from '@angular/core';
import { HttpClient, HttpParams } from '@angular/common/http';
import { environment } from '../environments/environment';
const databaseKey: any = environment;
@Injectable({
  providedIn: 'root'
})

export class ProductService {

  constructor() {

   }
}
